extends base.pug

block content 
    h5 Update Video
    //action is transmit the data what i wanna the place
    //what i'm using the POST method is send the file to something or change the value in the database
    //we ain't making the certain url simply because we're uing currently url i'm talking Don't take action method
    form(method="POST")
        input(name="title",placehoder="video Title",value=video.title,required)
        input(placeholder="Description",required,type="text",name="description",minlength=20,maxlength=140, value=video.description)
        input(placeholder="Hashtags, separated by comma.",required,type="text",name="hashtags",value=video.hashtags.join())
        input(value="Save",type="submit")
        